{
  "name": "thai-tm30-helper",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "pnpm clean && node scripts/build.mjs && node scripts/copy-static.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "xlsx": "^0.18.5"
  }
}
